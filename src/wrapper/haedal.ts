export interface NewMarketParams {
    expiry: number;
    scalar_root: string;
}

export class HasuiWrapper {
    NewMarket(coinTypePT: string, coinTypeSY: string, address: string) {
        
    }
}